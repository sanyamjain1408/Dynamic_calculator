import 'package:flutter/material.dart';

class CaDepreciationScreen extends StatelessWidget {
  const CaDepreciationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('CA Depreciation Calculator Screen')),
    );
  }
}